You are an expert software architect and a senior developer. Your task is to analyze the entire codebase of the current project in my VS Code workspace. Based on your analysis, generate a single, comprehensive, high-level prompt that can be used in the future to instruct an AI assistant (like yourself) to recreate this exact project from scratch.

The final output should be a single, detailed prompt inside a markdown code block.

To do this, please follow these steps:

1.  **Analyze the Project Structure:** Scan all folders and files to understand the directory layout.
2.  **Identify Core Technologies:** Determine the main programming languages, frameworks, and libraries being used (e.g., React, Node.js, Python, Django, etc.). Pay close attention to `package.json`, `requirements.txt`, or any other dependency management files.
3.  **Understand the Application's Purpose:** Analyze the code to determine the primary functionality of the application. What problem does it solve?
4.  **Detail the Components:** Describe the purpose of each key file and component. For example:
    *   What is the role of `App.js`?
    *   What do the services in the `/api` folder do?
    *   How are the UI components in the `/components` directory structured and styled?
5.  **Map Out the Logic:** Explain the business logic, data flow, and any state management (e.g., Redux, Context API).
6.  **Synthesize into a Master Prompt:** Combine all of the above information into one single, detailed prompt. This prompt should be a set of instructions for creating the project, including:
    *   The folder and file structure to create.
    *   The content for each file, including the full code.
    *   The necessary dependencies to install.

**BEGIN ANALYSIS AND GENERATE THE PROMPT BELOW:**

---

```markdown
**[Copilot will start generating the high-level prompt here]**
